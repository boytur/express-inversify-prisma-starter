DATABASE_URL="postgresql://user:password@localhost:5432/mydatabase"
JWT_SECRET="change-me-to-a-secure-value"
# Optional: port the app listens to
PORT=3000
## Node environment: development | production | test
NODE_ENV=development
## TLS/HTTPS should be handled by your reverse proxy/load balancer (e.g. nginx, cloud LB).
## The app listens on HTTP only. Configure TLS outside the application for production.
## Optional remote log collector endpoint. If set, the app will POST each log entry
# to this URL (non-blocking). Example: https://logs.example.com/ingest
LOG_ENDPOINT=
LOG_LEVEL=info
